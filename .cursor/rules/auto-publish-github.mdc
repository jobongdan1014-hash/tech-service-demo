---
description: 本仓库修改完成后自动提交并推送到 GitHub（main）
alwaysApply: true
---

# 自动发布到 GitHub

在本项目中完成**实质性代码或内容修改**后（例如 HTML/样式/脚本、文案、交互），除非用户明确说「不要提交」「仅本地」等，否则在任务结束前**必须**执行发布流程：

1. 在项目根目录执行 `git status`，确认变更文件。
2. 若 `index.html` 与 `移动端首页工作提醒消息通知和预警提示.html` 需保持一致，确保两者已同步后再提交。
3. 执行 `git add`（包含所有应纳入本次任务的变更）。
4. 使用简洁清晰的提交说明执行 `git commit`（中文或英文均可，说明「做了什么」）。
5. 执行 `git push origin main`。
6. 若推送失败（网络、鉴权），说明原因并给出可重试步骤；不要静默跳过。

若本次会话**没有**产生文件变更，或用户只要说明/答疑未改文件，则不必执行提交与推送。
